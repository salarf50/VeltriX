# زمینهٔ پروژهٔ VeltriX برای Manus

این فایل خلاصهٔ وضعیت پروژه است تا اگر حساب Manus تغییر کرد یا حساب جدیدی ساخته شد، کار بدون بررسی‌های تکراری ادامه پیدا کند.

## نقش مورد انتظار Manus

Manus باید به‌عنوان همکار فنی و عملیاتی پروژهٔ VeltriX عمل کند. کاربر معمولاً درخواست را به زبان فارسی و به‌صورت کوتاه بیان می‌کند؛ برای نمونه: «در ربات تلگرام فلان تغییر را بده»، «این بخش سایت را اصلاح کن» یا «ربات را دوباره اجرا کن». Manus باید ابتدا فایل و سرویس مرتبط را پیدا کند، وابستگی‌ها و تنظیمات را بررسی کند، تغییر را اعمال کند، تست‌های لازم را انجام دهد و نتیجه را گزارش کند.

برای عملیات تغییر‌دهنده، ابتدا باید دقیقاً مشخص شود چه چیزی تغییر می‌کند و از Secrets و Tokenها نباید خروجی یا مقدار واقعی نمایش داده شود. عملیات خواندنی را می‌توان بدون تأیید جداگانه انجام داد؛ برای حذف داده، تغییر دسترسی، تغییر امنیت حساب یا اقدام برگشت‌ناپذیر باید قبل از اجرا توقف و تأیید کاربر گرفته شود.

## هویت و پروژه

- نام برند: **VeltriX**
- حوزهٔ فعالیت: طراحی و ساخت قالب‌های دقیق تزریق پلاستیک و راهکارهای تولید صنعتی
- دامنهٔ اصلی: `https://veltrixmold.ir/`
- صفحهٔ Instagram: `https://www.instagram.com/veltrix_mold/`
- مخزن اصلی GitHub: `https://github.com/salarf50/VeltriX`
- مخزن خصوصی جانبی: `salarf50/kv-get-started`
- شاخهٔ اصلی مخزن VeltriX: `main`

## ساختار مخزن اصلی

مخزن `salarf50/VeltriX` شامل موارد زیر است:

- `index.html`: صفحهٔ اصلی سایت
- `articles/`: مقالات و قالب مقاله
- `assets/`: لوگو و تصاویر برند
- `content/`: مستندات و محتوای تکمیلی
- `telegram-bot/`: کد Workerها، فایل‌های Wrangler و مستندات ربات
- `.github/workflows/`: گردش‌کارهای Deploy
- `CONTENT-PLAN-30-DAY.md`: برنامهٔ محتوایی
- `sitemap.xml` و `robots.txt`: فایل‌های فنی SEO

آخرین بررسی نشان داده است که Deployهای سایت در GitHub Actions موفق بوده‌اند.

## Cloudflare

اتصال Cloudflare فعال است. حساب مورد استفاده دارای Zone فعال زیر است:

- Zone: `veltrixmold.ir`
- وضعیت: `active`
- نوع: `full`
- پلن فعلی: `Free Website`

Workerهای دیده‌شده در حساب:

- `veltrix-telegram-bot`: ربات اصلی تلگرام
- `veltrix-baleh-bot`: ربات اصلی بله
- `veltrix-telegram-test`: نسخهٔ آزمایشی تلگرام
- `veltrix-mini-test`: Worker آزمایشی
- `kv-get-started`: پروژهٔ آزمایشی/مرتبط با KV
- `little-lake-6810`: Worker دیگر موجود در حساب

هرگونه تغییر در Workerهای اصلی باید با توجه به کد GitHub و روش Deploy پروژه انجام شود تا نسخهٔ مستقر و کد منبع از هم جدا نشوند.

## ربات‌ها

### ربات تلگرام

Worker اصلی تلگرام در مسیر زیر است:

- `telegram-bot/worker.js`
- نسخه‌های مرتبط: `worker-full.js` و `worker-cloudflare.js`
- تنظیمات Wrangler: `telegram-bot/wrangler.toml`

قابلیت‌های طراحی‌شده:

- پشتیبانی از فارسی، انگلیسی، ترکی، عربی و آذری
- تشخیص زبان پیام
- ارائهٔ منوی خدمات
- دریافت اطلاعات پروژه و سرنخ مشتری
- ذخیرهٔ وضعیت گفت‌وگو و سرنخ‌ها در Cloudflare KV
- اطلاع‌رسانی سرنخ به چت مدیر
- ارسال اختیاری داده به Google Sheets از طریق Webhook
- پاسخ محدود به پرسش‌های فنی در صورت تنظیم Binding مربوط به AI
- مسیر سلامت: `GET /health`
- دریافت Webhook از مسیر محافظت‌شدهٔ تلگرام

### ربات بله

Worker اصلی بله:

- `veltrix-baleh-bot`
- تنظیمات و Workflow آن در `telegram-bot/wrangler-baleh.toml` و `.github/workflows/deploy-baleh-bot.yml` قرار دارد.

## Secrets و Bindingها

مقادیر واقعی Secretها نباید در گزارش، Commit یا فایل زمینه ذخیره شوند. نام‌های مورد انتظار در مستندات پروژه شامل این موارد است:

- `TELEGRAM_BOT_TOKEN`
- `WEBHOOK_SECRET`
- `ADMIN_CHAT_ID`
- `SHEETS_WEBHOOK_URL` (اختیاری)
- Binding مربوط به `LEADS_KV` (اختیاری)
- Binding مربوط به `AI` (اختیاری)

این نام‌ها فقط برای شناسایی تنظیمات هستند؛ مقدار آن‌ها را از کاربر درخواست نکن مگر برای تنظیم مجدد ضروری باشد و هرگز آن‌ها را در خروجی عمومی چاپ نکن.

## سایت

سایت فعلی دربارهٔ قالب‌های تزریق پلاستیک دقیق است و بخش‌هایی مانند این موارد دارد:

- Hot Runner
- قالب قطعات پزشکی
- قالب بسته‌بندی
- قالب‌های چندکویته
- Valve Gate
- قالب دایکاست
- قالب قطعات خودرویی
- قالب لوازم خانگی
- Thermoforming
- قطعات مهندسی پلاستیکی
- پروژه‌های صادراتی برای ترکیه، عراق، امارات و اروپا

مسیرهای ارتباطی سایت شامل Instagram با نام `@veltrix_mold`، ربات تلگرام `@veltrixmold_bot`، ربات بله `ble.ir/veltrixmold_bot` و ادامهٔ گفتگو در WhatsApp است.

## وضعیت اتصال‌ها در حساب فعلی

- GitHub: فعال و قابل استفاده برای مشاهدهٔ مخزن، بررسی تغییرات و گردش‌کارها.
- Cloudflare: فعال و قابل استفاده برای مشاهده و مدیریت منابع مجاز حساب.
- Google Workspace: در بررسی اخیر غیرفعال بود؛ پروژه ممکن است از Google Sheets Webhook ذخیره‌شده در Secret استفاده کند.
- Instagram: اتصال مستقیم برای مدیریت پست/استوری/پیام در بررسی اخیر فعال نبود؛ صفحهٔ عمومی از داخل سایت قابل شناسایی است.

در حساب جدید، ابتدا باید فعال‌بودن GitHub و Cloudflare بررسی شود. اگر لازم است به Drive، Sheets یا Docs مستقیماً دسترسی داده شود، اتصال Google Workspace نیز باید توسط کاربر فعال شود.

## روش ادامهٔ کار

برای هر درخواست جدید:

1. مشخص کن درخواست مربوط به سایت، GitHub، Cloudflare Worker، KV، ربات تلگرام، ربات بله یا محتوای Instagram است.
2. فایل‌ها و تنظیمات مرتبط را در مخزن پیدا کن.
3. قبل از تغییر، وضعیت فعلی و مسیر Deploy را بررسی کن.
4. تغییر را در منبع اصلی GitHub اعمال کن، نه فقط به‌صورت دستی در Worker مستقر؛ مگر اینکه کاربر صریحاً تعمیر مستقیم بخواهد.
5. تست مناسب اجرا کن؛ برای سایت، بررسی HTTP و برای Worker، بررسی `/health` یا مسیر مربوط.
6. وضعیت GitHub Actions و در صورت امکان وضعیت Worker را بررسی کن.
7. در پایان به فارسی گزارش بده: چه چیزی تغییر کرد، کدام فایل‌ها تغییر کردند، چه تستی انجام شد و آیا Deploy موفق بود.

## نکتهٔ مهم

این فایل شامل هیچ Token، رمز عبور، Secret، کلید API یا Webhook URL واقعی نیست. در صورت ایجاد حساب جدید Manus، این فایل را به‌همراه لینک مخزن اصلی بده و سپس اتصال‌های GitHub و Cloudflare را فعال کن.

آخرین به‌روزرسانی این خلاصه: 2026-09-24
نویسندهٔ خلاصه: Manus
حالت بررسی اولیه: فقط خواندنی؛ در زمان تهیهٔ این فایل هیچ تغییر کدی یا تنظیماتی اعمال نشده است.


## قانون قطعی دربارهٔ تعداد دنبال‌کنندگان Instagram

هر زمان کاربر گفت تعداد دنبال‌کنندگان Instagram را در سایت به‌روزرسانی کن، ترتیب کار الزامی است: ابتدا صفحهٔ واقعی `https://www.instagram.com/veltrix_mold/` بررسی شود، سپس عددی که همان‌جا مشاهده می‌شود در سایت ثبت شود. نتیجهٔ جست‌وجوی وب، عدد قبلی سایت یا حدس تقریبی به‌تنهایی معتبر نیست. اگر Instagram از محیط قابل مشاهده نبود یا صفحهٔ ورود نشان داد، نباید عددی حدس زده یا تغییر داد؛ باید از کاربر اسکرین‌شات/عدد فعلی خواست یا از او خواست اتصال/مرورگر واردشده را فعال کند. پس از مشاهدهٔ عدد واقعی، مقدار در `index.html` تغییر کند، تست شود، Commit و Deploy شود و نتیجه گزارش گردد.


## شناسنامهٔ صفحهٔ Instagram بر اساس اسکرین‌شات مالک

منبع این بخش: اسکرین‌شات ارسالی مالک در تاریخ 2026-09-24، ساعت حدود 13:08. صفحهٔ موردنظر با شناسهٔ `@veltrix_mold` و نام نمایشی `veltrix` شناسایی شد. در اسکرین‌شات، صفحهٔ حرفه‌ای با دستهٔ **Commercial & Industrial** دیده می‌شود و اطلاعات قابل‌تأیید آن چنین است:

| مورد | مقدار مشاهده‌شده |
|---|---|
| شناسهٔ صفحه | `veltrix_mold` |
| نام نمایشی | `veltrix` |
| تعداد پست‌ها | 45 |
| دنبال‌کنندگان | 7,230 |
| دنبال‌شوندگان | 1,169 |
| دستهٔ حساب | Commercial & Industrial |
| نام برند در Bio | Veltrix |
| معرفی برند | Precision Injection Molds \| Export Quality |
| خدمات ذکرشده | Hot Runner · Valve Gate · Medical · Packaging |
| بازارهای ذکرشده | ترکیه · عراق · امارات · روسیه |
| لینک تماس قابل مشاهده | WhatsApp با پیش‌شمارهٔ ایران؛ ادامهٔ لینک در تصویر کامل نیست |
| وضعیت حرفه‌ای | Professional Dashboard |
| آمار داشبورد | 458.8K بازدید در 30 روز اخیر |

در صفحه، هایلایت‌ها/دسته‌های محتوایی مربوط به Reels، پروژه‌ها، Contact و محتوای شهریور دیده می‌شود. در شبکهٔ پست‌های قابل مشاهده، ویدیوهای کوتاه آموزشی و معرفی محصولات/قالب‌ها دیده می‌شود؛ نمونه‌های قابل خواندن روی اسکرین‌شات شامل موضوعاتی مانند آموزش قالب ترموفرمینگ، بررسی آماری و طراحی/تولید قالب پلاستیکی است. اعداد بازدید قابل مشاهده برای سه ویدیوی شبکه در تصویر حدود 168K، 13.9K و 5,228 بوده‌اند.

این شناسنامه بر اساس اسکرین‌شات است و جایگزین دسترسی مستقیم به Instagram API یا پنل حساب نیست. در بررسی مستقیم از محیط Manus، Instagram صفحهٔ عمومی را به‌طور پایدار نمایش نداد و گاهی صفحهٔ ورود یا پیام Profile isn’t available نشان داد؛ بنابراین برای عددهای بعدی، اسکرین‌شات تازه یا اتصال معتبر Instagram لازم است.

## برنامهٔ همکاری آینده با VeltriX و Instagram

هدف همکاری این است که Manus همکار فنی، محتوایی و عملیاتی VeltriX باشد. در بخش سایت، کارها شامل اصلاح متن و ساختار صفحات، افزودن مقالات و ویدیوهای آپارات، بهبود دعوت به ارسال پروژه، بررسی لینک‌های تماس و انتشار کنترل‌شده از GitHub Pages است. در بخش ربات‌ها، کارها شامل نگهداری و توسعهٔ Workerهای تلگرام و بله، اصلاح جریان دریافت سرنخ، بررسی سلامت webhook، مدیریت KV و هماهنگی با تنظیمات Cloudflare است. در بخش محتوا، می‌توان تقویم محتوایی، ایده‌های Reels، کپشن‌های مشتری‌پسند و اتصال موضوعات Instagram به خدمات سایت را برنامه‌ریزی کرد.

هر زمان ویدیوی جدیدی برای سایت یا محتوای مرتبط با VeltriX مطرح شد، ابتدا صفحهٔ منبع آپارات بررسی شود و بعد عنوان، موضوع و توضیحات آن با هدف مشتری‌پسند بازبینی دو مرحله‌ای شود. کپشن نهایی باید روشن، حرفه‌ای، قابل‌اعتماد و متناسب با نیاز مشتری صنعتی باشد؛ از وعدهٔ قطعی دربارهٔ قیمت، زمان تحویل، گواهی یا نتیجهٔ مهندسی بدون تأیید تیم فنی پرهیز شود. کپشن باید در صورت تناسب، مخاطب را به ارسال نقشه، فایل سه‌بعدی یا توضیحات پروژه دعوت کند.

قانون تعداد دنبال‌کنندگان پابرجاست: ابتدا صفحهٔ واقعی Instagram بررسی شود و فقط عدد مشاهده‌شده در همان صفحه، یا عددی که مالک با اسکرین‌شات تازه تأیید کرده، در سایت ثبت شود. نتیجهٔ جست‌وجوی وب، عدد قبلی سایت یا تخمین گردشده معتبر نیست. در صورت نبود دسترسی، هیچ عددی حدس زده نشود.

## روند استاندارد هر تغییر

ابتدا منبع داده یا صفحهٔ اصلی بررسی شود؛ سپس فایل مرتبط در GitHub پیدا شود؛ بعد تغییر در منبع اصلی اعمال و با `git diff --check` و بررسی محتوایی تست شود؛ در ادامه Commit و Push انجام شود؛ پس از آن وضعیت GitHub Actions و نسخهٔ آنلاین بررسی گردد. گزارش نهایی باید شامل منبع بررسی‌شده، تغییرات، Commit، نتیجهٔ تست و وضعیت Deploy باشد. عملیات حذف، تغییر Secret، تغییر دسترسی، تغییر DNS یا اقدام برگشت‌ناپذیر بدون تأیید روشن کاربر انجام نشود.

آخرین به‌روزرسانی این فایل: 2026-09-24


## قانون قطعی همگام‌سازی دو ربات

هر تغییری که کاربر برای ربات درخواست کند، باید هم‌زمان در هر دو ربات اصلی اعمال شود: **ربات تلگرام `veltrix-telegram-bot`** و **ربات بله `veltrix-baleh-bot`**. این قانون شامل تغییر منو، متن‌ها، جریان گفت‌وگو، فرم جمع‌آوری لید، ذخیره‌سازی KV، ارسال اطلاعات به ادمین، وضعیت لید، پاسخ‌گویی، تنظیمات مرتبط و تست/Deploy است. قبل از پایان هر مأموریت باید هر دو فایل Worker، تست نحوی هر دو، Diff، Commit/Push و وضعیت Deploy هر دو بررسی شود. اگر تغییر در یکی از پلتفرم‌ها به‌دلیل تفاوت API نیازمند پیاده‌سازی متفاوت باشد، رفتار و نتیجهٔ کسب‌وکاری باید در هر دو ربات معادل باقی بماند و تفاوت فنی در گزارش نهایی ذکر شود.
