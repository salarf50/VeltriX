# خلاصهٔ انتقال پروژهٔ VeltriX برای Manus

## هدف همکاری
تمرکز همکاری روی توسعه و نگهداری **سایت VeltriX** و **دو ربات تلگرام و بله** است. تولید و مدیریت محتوای Instagram بر عهدهٔ مالک پروژه است و Manus فقط وقتی دربارهٔ آن نظر یا اقدامی ارائه می‌دهد که کاربر صریحاً درخواست کند.

## هویت پروژه
- برند: VeltriX
- حوزه: طراحی و ساخت قالب‌های دقیق تزریق پلاستیک، هات‌رانر، ولوگیت، قالب پزشکی و بسته‌بندی، پرینت سه‌بعدی و مشاوره فنی
- سایت: https://veltrixmold.ir/
- GitHub: https://github.com/salarf50/VeltriX
- شاخه: `main`
- صفحهٔ Instagram برای بررسی عدد فالوور در صورت درخواست: https://www.instagram.com/veltrix_mold/

## فایل‌ها و استقرار
- سایت: `index.html` و پوشه‌های `assets/`, `articles/`, `content/`
- Worker تلگرام: `telegram-bot/worker-full.js`
- Worker بله: `telegram-bot/worker-baleh.js`
- تنظیمات: `telegram-bot/wrangler.toml` و `telegram-bot/wrangler-baleh.toml`
- استقرار از GitHub Actions انجام می‌شود؛ تغییر هر دو ربات باید هم‌زمان اعمال و هر دو Deploy بررسی شوند.

## قابلیت‌های فعلی ربات‌ها
- زبان‌ها: فارسی، انگلیسی، ترکی، عربی و آذری
- شروع چندزبانه، انتخاب زبان و منوی خدمات
- خدمات فعلی: پرینت سه‌بعدی، مشاوره و شروع بررسی پروژه
- دریافت مرحله‌ای نام، شرکت، شماره تماس، شهر/کشور، نوع قطعه، تیراژ و فایل/عکس
- ارسال لید به ادمین و ذخیره در Cloudflare KV
- دکمهٔ مسیریابی نشان: https://nshn.ir/rbAyVnpCdN0H
- شماره تماس: `+989141026956`، پاسخ‌گویی ۸ صبح تا ۷ عصر
- اعلام پاسخ‌گویی در بله، روبیکا، واتساپ و دایرکت Instagram؛ برای پاسخ سریع‌تر استفاده از ربات پیشنهاد می‌شود.

## گزارش روزانهٔ جدید
هر دو ربات رویدادهای زیر را در KV ثبت می‌کنند:
- چه کسی `/start` یا منو را باز کرده است
- چه زبانی انتخاب کرده است
- کدام خدمت را انتخاب کرده است
- در کدام مرحلهٔ فرم باقی مانده است

در پایان هر روز به وقت ایران، گزارش روز قبل برای ادمین همان ربات ارسال می‌شود و شامل این موارد است:
- تعداد شروع‌کنندگان یکتا
- تعداد انتخاب‌کنندگان خدمت
- نام/شناسه/چت‌آیدی قابل‌دسترسی
- زبان و خدمت انتخاب‌شده
- فهرست افراد رهاکرده و مرحلهٔ رهاشده

برای عبور از سقف cron رایگان Cloudflare، هر Worker فقط یک Trigger زمان‌بندی‌شده دارد (`30 20 * * *` UTC، حدود نیمه‌شب به وقت ایران). همان Trigger گزارش روزانه، یادآوری پرونده‌های باز و بررسی هفتگی را اجرا می‌کند.

## کاربرد پرامپت جذب مشتری
پرامپت جذب مشتری فقط برای خروجی‌های مربوط به سایت و ربات‌ها استفاده شود:
- طراحی صفحات و CTAهای سایت برای درخواست بررسی پروژه
- نوشتن متن فرم‌ها و سوالات qualification
- طراحی جریان لیدگیری و کاهش رهاکردن مراحل
- ساخت پاسخ‌های اعتراض مشتری و پیام‌های پیگیری برای ادمین
- پیشنهاد شاخص‌های Analytics و Search Console برای سایت
- برنامه‌ریزی تست و بهبود مسیر تبدیل بازدیدکننده به لید

فعلاً برای تولید محتوا، کپشن، Reel یا برنامهٔ Instagram خروجی تولید نشود مگر با درخواست مستقیم مالک.

## قوانین اجرایی
1. هر تغییر ربات باید در هر دو Worker اعمال شود.
2. قبل از تغییر، فایل منبع و مسیر Deploy بررسی شود.
3. پس از تغییر، `node --check` برای هر دو Worker و `git diff --check` اجرا شود.
4. Secret، Token و Webhook واقعی هرگز در گزارش یا فایل انتقال نوشته نشود.
5. برای تغییرات حذف‌کننده، امنیتی یا دسترسی حساب، ابتدا تأیید روشن کاربر گرفته شود.
6. برای تعداد فالوور Instagram، ابتدا صفحهٔ واقعی بررسی شود؛ اگر Instagram صفحهٔ ورود نشان داد، عدد حدس زده نشود و از کاربر اسکرین‌شات تازه درخواست شود.

## آخرین تغییر مهم
Commit مربوط به گزارش روزانه: `4d41233`.
پس از آن، به‌دلیل سقف cron رایگان Cloudflare، تنظیمات زمان‌بندی به یک Trigger مشترک برای هر Worker کاهش داده شده و باید با Commit بعدی Deploy شود.
